//Need to create the Store class (similar to last weeks homework).
//Add methods for reading.  Read Note: Get Note: read from the file

//needs the util and fs packages at the top with require

//then add uuidv1 = require ("uuid/v1")

//class Store {read(){return readFileAsyn("db/db.json", "utf8")}}
//write(note){}

module.exports = new Store();
